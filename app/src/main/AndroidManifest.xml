<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.socra.socra.retro_freak">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_retro"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_retro_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activitis.MainActivity" />
        <activity
            android:name=".activitis.Dragon_ball"
            android:parentActivityName=".activitis.MainActivity" />
        <activity
            android:name=".activitis.Naruto"
            android:parentActivityName=".activitis.MainActivity" />
        <activity
            android:name=".activitis.pantalla_carga"
            android:theme="@style/pantalla_carga">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activitis.activiti_gogeta"
            android:parentActivityName=".activitis.MainActivity" />
        <activity
            android:name=".activitis.ninitendo"
            android:parentActivityName=".activitis.MainActivity" />
        <activity
            android:name=".activitis.final_fantasy"
            android:parentActivityName=".activitis.MainActivity" />
        <activity
            android:name=".activitis.juego_de_tronos"
            android:parentActivityName=".activitis.MainActivity" />
        <activity
            android:name=".activitis.bigbang"
            android:parentActivityName=".activitis.MainActivity"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activitis.MapsActivity"
            android:label="@string/title_activity_maps"></activity>
    </application>

</manifest>